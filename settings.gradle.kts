pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}

rootProject.name = "buildopt-pilot"

include(
    "lib01", "lib02", "lib03", "lib04",
    "lib05", "lib06", "lib07", "app",
)
